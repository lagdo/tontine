@if ($currentGuild !== null)
                  <span style="font-weight:700; font-size:17px;">
                    <i class="fa fa-money-bill"></i> {{ $locale->getCurrencyName() }}
                  </span>
@endif
